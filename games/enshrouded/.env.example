# Copied to /etc/game-server/.env by the bake script. docker compose reads
# this automatically when the working directory is /etc/game-server.

SERVER_NAME=My Shroud
SERVER_PASSWORD=changeme
SERVER_SLOTS=4
