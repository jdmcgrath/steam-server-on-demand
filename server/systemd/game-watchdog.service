[Unit]
Description=Game server idle watchdog (A2S player-count probe)
After=docker.service
Requires=docker.service

[Service]
Type=simple
ExecStart=/usr/local/bin/game-watchdog
EnvironmentFile=-/etc/game-server/.env
Restart=always

[Install]
WantedBy=multi-user.target
